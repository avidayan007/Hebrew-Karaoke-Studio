(()=>{
 const $=id=>document.getElementById(id), frame=()=>$('console');
 const HKEY='afdConsoleHeightV60', SKEY='afdScreenSizesV60';
 let sizes={};try{sizes=JSON.parse(localStorage.getItem(SKEY)||'{}')||{}}catch(e){sizes={}};
 const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
 function saveSizes(){localStorage.setItem(SKEY,JSON.stringify(sizes))}
 function addParentDivider(){
  const f=frame(),dock=document.querySelector('.dock');if(!f||!dock||document.getElementById('afdMainDivider'))return;
  const saved=+localStorage.getItem(HKEY);if(saved>400)f.style.height=saved+'px';
  dock.style.marginTop='0';
  const bar=document.createElement('div');bar.id='afdMainDivider';bar.title='גרור למעלה/למטה כדי לשנות את גובה החלק העליון';
  bar.innerHTML='<span>⋮⋮ גרור לגובה ⋮⋮</span>';
  const st=document.createElement('style');st.id='afdDividerStyle';st.textContent=`#afdMainDivider{height:12px;margin:0;background:linear-gradient(#252c35,#090c10);border-top:1px solid #66717d;border-bottom:1px solid #343c45;cursor:ns-resize;display:grid;place-items:center;position:relative;z-index:20;touch-action:none;user-select:none}#afdMainDivider span{font-size:8px;color:#9aa6b3;letter-spacing:2px;pointer-events:none}.dock{margin-top:0!important}`;document.head.appendChild(st);
  f.insertAdjacentElement('afterend',bar);
  let sy=0,sh=0,drag=false,lastH=0,raf=0;
  const move=e=>{if(!drag)return;const y=e.clientY??e.touches?.[0]?.clientY;if(y==null)return;lastH=clamp(sh+(y-sy),430,850);if(!raf)raf=requestAnimationFrame(()=>{raf=0;f.style.height=lastH+'px';f.style.minHeight=lastH+'px'});e.preventDefault?.()};
  const up=()=>{drag=false;document.body.style.userSelect='';if(lastH)localStorage.setItem(HKEY,String(Math.round(lastH)))};
  bar.addEventListener('pointerdown',e=>{drag=true;sy=e.clientY;sh=f.getBoundingClientRect().height;lastH=sh;bar.setPointerCapture?.(e.pointerId);document.body.style.userSelect='none';e.preventDefault()});
  bar.addEventListener('pointermove',move);bar.addEventListener('pointerup',up);bar.addEventListener('pointercancel',up);
 }
 function deckDoc(){try{return frame()?.contentDocument||null}catch(e){return null}}
 function fixCrossLabels(d){const cross=d.querySelector('.master .cross');if(!cross)return;const kids=[...cross.children];if(kids.length>=3){kids[0].textContent='B';kids[kids.length-1].textContent='A'}}
 function startDeck(d,deck){const b=d.querySelector(`[data-act="play"][data-d="${deck}"]`);if(b){b.click();return}const m=d.getElementById('vid'+deck);if(m?.play)m.play().catch(()=>{})}
 function wireMix(d){const b=d.getElementById('afdMixBtn');if(!b||b.dataset.afd60)return;b.dataset.afd60='1';b.textContent='MIX B ↔ A';const cross=d.getElementById('cross'),video=d.getElementById('videoCross')||d.querySelector('.master .cross input[type=range]');b.onclick=()=>{if(b.dataset.mixing==='1')return;const from=+(cross?.value||50),to=from<50?100:0,target=to===100?'A':'B';startDeck(d,target);b.dataset.mixing='1';b.classList.add('on');const start=performance.now(),dur=6000;const tick=now=>{const t=Math.min(1,(now-start)/dur),v=from+(to-from)*t;if(cross){cross.value=v;cross.dispatchEvent(new Event('input',{bubbles:true}))}if(video&&video!==cross){video.value=v;video.dispatchEvent(new Event('input',{bubbles:true}))}if(t<1)requestAnimationFrame(tick);else{b.dataset.mixing='0';b.classList.remove('on')}};requestAnimationFrame(tick)}}
 function addScreenHandle(d,el,key){if(!el||el.querySelector(':scope > .afdResizeHandle'))return;el.style.position='relative';if(sizes[key]){if(sizes[key].h)el.style.height=sizes[key].h+'px';if(sizes[key].w)el.style.width=clamp(sizes[key].w,160,el.parentElement?.clientWidth||9999)+'px'}const h=d.createElement('div');h.className='afdResizeHandle';h.title='גרור כדי לשנות גודל';el.appendChild(h);let sx=0,sy=0,sw=0,sh=0,drag=false,nw=0,nh=0,raf=0;h.addEventListener('pointerdown',e=>{drag=true;sx=e.clientX;sy=e.clientY;const r=el.getBoundingClientRect();sw=r.width;sh=r.height;nw=sw;nh=sh;h.setPointerCapture?.(e.pointerId);e.preventDefault();e.stopPropagation()});h.addEventListener('pointermove',e=>{if(!drag)return;const maxW=Math.max(180,(el.parentElement?.clientWidth||sw));nw=clamp(sw+(e.clientX-sx),160,maxW);nh=clamp(sh+(e.clientY-sy),90,420);if(!raf)raf=requestAnimationFrame(()=>{raf=0;el.style.width=nw+'px';el.style.height=nh+'px'});e.preventDefault()});const finish=()=>{if(!drag)return;drag=false;sizes[key]={w:Math.round(nw),h:Math.round(nh)};saveSizes()};h.addEventListener('pointerup',finish);h.addEventListener('pointercancel',finish)}
 function addScreenControls(d){if(!d.getElementById('afdResizeStyle')){const s=d.createElement('style');s.id='afdResizeStyle';s.textContent=`.afdResizeHandle{position:absolute!important;right:1px!important;bottom:1px!important;width:18px!important;height:18px!important;z-index:50!important;cursor:nwse-resize!important;touch-action:none!important;background:linear-gradient(135deg,transparent 0 35%,#bac4ce 36% 45%,transparent 46% 56%,#bac4ce 57% 66%,transparent 67%)!important;border-radius:2px!important;opacity:.8!important}.screen,.masterScreen{min-width:160px!important;max-width:100%!important}.afdLayoutReset{position:absolute;right:7px;top:7px;z-index:60;height:24px;border:1px solid #69737e;border-radius:4px;background:#11161c;color:#cfd7df;font-size:7px;font-weight:900}`;d.head.appendChild(s)}addScreenHandle(d,d.querySelector('.deckA .screen'),'A');addScreenHandle(d,d.querySelector('.deckB .screen'),'B');addScreenHandle(d,d.querySelector('.masterScreen'),'M');const master=d.querySelector('.master');if(master&&!d.getElementById('afdLayoutReset')){const r=d.createElement('button');r.id='afdLayoutReset';r.className='afdLayoutReset';r.textContent='RESET SIZE';r.onclick=()=>{sizes={};localStorage.removeItem(SKEY);[d.querySelector('.deckA .screen'),d.querySelector('.deckB .screen'),d.querySelector('.masterScreen')].forEach(x=>{if(x){x.style.removeProperty('width');x.style.removeProperty('height')}})};master.appendChild(r)}}
 function applyInner(){const d=deckDoc();if(!d)return;fixCrossLabels(d);wireMix(d);addScreenControls(d)}
 function run(){addParentDivider();applyInner()}
 frame()?.addEventListener('load',()=>{setTimeout(run,250);setTimeout(run,900)});setTimeout(run,500);setTimeout(run,1400);window.addEventListener('resize',()=>setTimeout(run,80),{passive:true});
})();